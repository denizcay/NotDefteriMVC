@model List<Note>
@{
    ViewData["Title"] = "Not Defteri";
}

<a asp-controller="Notlar" asp-action="Yeni" class="btn btn-primary">Yeni Not Ekleme</a>

@if (Model.Count == 0)
{
    <p>Gösterilecek bir not bulunmuyor.</p>
}
else
{
    <ul>
        @foreach (Note n in Model)
        {
            <li>
                <strong>@n.Title</strong> <br />
                @n.Content
            </li>
        }
    </ul>
}


